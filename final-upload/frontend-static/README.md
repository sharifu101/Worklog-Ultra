## Frontend Static Assets

This folder is optional.

Use it only if:

- you want Nginx/CDN to serve static files separately
- you know how to map `/_next/static` and `/public`

If you do not need that, ignore this folder and deploy only `backend`.
