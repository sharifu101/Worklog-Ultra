## Backend Upload

Upload this whole folder to your Node.js hosting/server.

Then:

1. place your production `.env` in this folder
2. run `node server.js`

This package already contains:

- standalone Next.js server
- production node modules
- app routes and APIs
- uploaded public files

You still need:

- PostgreSQL database
- correct `.env`
- persistent uploads
